# Academy — 目标模板设计规范

> 版本：v1.1
> 原则：**Nothing counts unless it is evidenced.（没有证据，就不算完成。）**

---

## 1. 用途与边界

目标模板不是“让 AI 给用户生成一个学习计划”的提示词。它是 Academy 中可复用、可验证、可运营的受控学习—构建闭环。

只有通过本规范的目标，才可以进入正式 Academy 路径。GitHub 仓库、用户随口提出的愿望、模型生成的一串课程，均不能直接成为目标模板。

```text
目标（Goal）
  → 可交付物（Artifact）
  → 完成定义（Definition of Done）
  → 胜任力节点（Competency Nodes）
  → 里程碑（Checkpoints）
  → 今日行动（Today Actions）
  → 证据（Evidence）
  → 复核与进度（Reviewable Progress）
```

## 2. 准入标准

一个模板必须同时满足以下六项：

1. **明确的最终可交付物**：代码仓库、可运行原型、演示视频、分析报告、真实对话记录等至少一种，不能只是“学会 LangGraph”。
2. **明确的 Definition of Done**：可由他人复现和判断，不能写“用户感觉掌握”。
3. **有限的胜任力节点**：可拆为有限、带先修关系的节点；首版建议 6–12 个，不接受无限知识树。
4. **每个节点至少一种证据**：每个节点必须说明证据类型、采集位置、最低通过条件与是否需要复核。
5. **短反馈周期**：完整模板控制在 14–28 天；首轮目标模板固定为 21 天，并至少每 2–3 天设置一个可观察检查点。
6. **可在目标用户现实环境完成**：不得要求用户不存在的付费软件、专用设备、敏感生产数据或无法取得的第三方授权。

任一项不满足，模板进入“研究素材”，不进入用户路径。

## 3. Evidence Model（证据模型）

证据不是附件清单，而是对某个胜任力节点的可审计证明。一个胜任力通常需要多个证据共同支持。

| 证据类型 | 证明什么 | 常见采集方式 | 是否足以单独证明胜任力 |
|---|---|---|---|
| Quiz | 理解关键概念与边界 | 3–5 道关键知识题 | 否 |
| 项目产物 | 能将知识用于构建 | 工作流、代码、文档、配置 | 通常否 |
| Git Commit | 有可追溯的亲自实施过程 | 提交记录、Diff、仓库链接 | 否 |
| Demo Video | 能展示工作结果与解释设计 | 1–3 分钟演示视频 | 通常否 |
| Peer Review | 他人能按标准复核 | 同行结构化检查表 | 否，除非模板明确规定 |
| Mentor Review | 专家按量规复核 | 审核意见与结论 | 可作为强证据 |
| Runtime Success | 在规定输入下真实运行 | 测试日志、部署链接、成功率 | 可作为强证据 |
| Reflection | 能解释选择、失败与改进 | 固定复盘问题 | 否 |

### 3.1 Evidence Pyramid（证据金字塔）

```text
          Mentor Review
             ▲
           Peer Review
             ▲
        Runtime Evidence
             ▲
        Project Evidence
             ▲
             Quiz
```

越向上，证据越接近真实能力与外部可复核结果。MVP 至少达到 **Runtime Evidence**：不仅保存工作流或截图，还要在预设输入下得到可重复的运行结果。Mentor 与 Peer Review 均后置，不能成为首轮 10 人验证的运营前提。

### 3.2 证据采集与验证必须分离

- **Collected（已提交）**：文件、链接、视频或工作流已上传；
- **Rule-verified（规则验证）**：文件格式、时长、字段、工作流结构和预设运行测试符合规则；
- **Reviewed（复核通过）**：人工或未来的结构化审查确认质量与解释一致。

README 字数、视频时长、截图存在与 Git Commit 数量只能构成 Collected 或部分 Rule-verified；它们不能单独证明回答正确、引用真实或用户理解设计。

### 3.3 证据权重规则

- 权重属于**模板定义**，不是模型即时判断；
- 同一类证据不应占某项核心胜任力的 100%；
- 对“能构建”类胜任力，Quiz 建议不超过 20%，项目/运行类证据合计至少 50%；
- 进度为已验证权重之和，不等同于“已经掌握”；
- 证据必须保存来源、时间、模板版本、评分依据、复核状态与失效条件；
- 无法采集或复核的证据，权重为 0。

示例：`能够完成一个基础 AI 工作流` 可由 Quiz 20% + 已保存工作流 30% + Git Commit 15% + Runtime Success 25% + Reflection 10% 构成。此例只说明结构，具体权重须经模板评审确定。

## 4. Competency Graph（胜任力图谱）

`Competency` 表示“能够独立完成什么”，而非一个孤立工具名。

```text
胜任力：独立构建基础 AI 助手
├── 能定义输入、输出与限制条件
├── 能调用模型或工具
├── 能设计最小工作流
├── 能用测试问题验证结果
└── 能解释失败边界与人工介入点
```

每个节点应包含：节点 ID、描述、先修节点、对应课程或 Lab、证据要求、权重、复习规则、失效规则和人工复核要求。

禁止把“观看第 3 节课”或“登录某工具”当作胜任力节点。

## 5. 21 天模板骨架：Build a Personal Knowledge Assistant

首个模板的目标：**在 21 天内完成一个可运行、可演示的个人知识助手。**

首版使用 Academy 托管或指定的 **Flowise 无代码模板**；不要求 Git、Docker、LangGraph、Python、Telegram Bot、Webhook 或自行部署。高级技术栈属于后续 Advanced Track，不能污染首个模板的完成率。

| 检查点 | 结果而非学习时长 | 最低证据 |
|---|---|---|
| Day 0 — Environment Ready | Flowise 模板、模型入口、测试文档与 3 个问题就绪 | 环境检查、测试文档与问题 |
| Day 1 — Hello Retrieval | 上传指定文档，提出问题并得到回答 | 运行日志、概念 Quiz |
| Day 3 — Source Boundary | 定义助手只回答哪些资料、何时明确说“不知道” | 输入/输出契约、边界复盘 |
| Day 5 — Document Ingestion | 上传并索引至少一份可公开验证的文档 | 数据源配置、成功与失败样例 |
| Day 7 — First Working Prototype | 对 3 个预设问题返回答案与来源引用 | 自动运行测试通过、引用字段记录 |
| Day 10 — Quality Check | 识别无依据回答、错误引用或异常输出 | 错误样例、修正策略、Quiz |
| Day 14 — Usable Prototype | 对指定文档集连续回答问题并引用来源 | 至少 10 个测试、成功率记录 |
| Day 18 — Demo Ready | 完成使用说明与 2 分钟演示脚本 | README/使用说明、Demo 草稿 |
| Day 21 — Demo Day | 他人可上传文档、提问、查看答案和来源 | Flowise 导出或可访问链接、Demo 视频、测试报告、复盘 |

### 5.1 Definition of Done

首个模板只有同时满足以下条件才算完成：

- 原型在规定环境中可运行；
- 可以上传指定文档、提问、回答并显示来源引用；
- 有明确输入、输出、失败边界与人工介入说明；
- 通过模板规定的测试集，保存原始测试结果；
- 提供使用说明，使第三方能按步骤复现；
- 提供演示视频或现场演示；
- Flowise 工作流导出或模板版本可追溯；
- 用户能用自己的话解释核心设计、至少一个失败案例和下一步改进；
- 所有必需证据已记录并通过模板规则或人工复核。

“完成了课程”“打开过 Lab”“AI 说可以”均不构成 Done。

### 5.2 每日 Mission 与 Recovery Loop

首页每天只给出一个 Mission：一个可在当天完成、能直接推进下个检查点的动作。Mission 应展示“为什么是这一步”“完成后解锁什么证据”，而非只展示 Day 编号。

```text
Evidence Fail / 检查点未完成
        ↓
定位固定能力缺口（例如：资料未索引、引用字段缺失、JSON 配置错误）
        ↓
Recovery Mission（短讲解 + 一个修复动作 + 重新测试）
        ↓
证据重新验证
        ↓
通过后回到主路径
```

MVP 不跳过失败节点，也不永久锁死用户。Recovery Mission 必须来自预先定义的缺口—补救映射；模型可以解释，不得自由发明补课路径或降低通过标准。

## 6. 模板设计与上线流程

1. 提交模板卡：目标、受众、前置条件、周期、可交付物、DoD、节点、证据与风险；
2. 内容评审：核对事实、先修关系、许可证、成本、设备与移动端可行性；
3. 内部试跑：至少由创始人完整跑一遍，记录实际时长、阻塞和证据缺口；
4. 种子试跑：5–10 名目标用户中以小样本运行，观察完成、失败与求助点；
5. 发布版本：固定模板版本、评分量规与证据权重；
6. 复盘升级：只能创建新版本，不得静默改变在途用户的 DoD、权重或毕业标准。

## 7. 模板卡最小字段

```text
template_id / version
目标用户与前置条件
目标与可交付物
Definition of Done
预计周期与检查点
Competency Nodes 与先修关系
每日学习 / 构建 / 检查动作
Evidence 要求、权重、采集方式与复核规则
评分量规与失败处理
课程、资料和 Lab 引用
成本、许可证、隐私与安全约束
模板负责人、审核记录与变更日志
```

## 8. 禁止项

- 允许模型为每个用户自由重写整个课程、证据权重或完成定义；
- 使用浏览、阅读时长、打开次数或 AI 主观评价作为完成证据；
- 以单次 Quiz、单次截图或单个 Git Commit 宣称用户已具备复合胜任力；
- 将未经审核的 GitHub 内容直接投入正式模板；
- 没有 DoD、检查点、证据或失败处理策略就发布模板。
